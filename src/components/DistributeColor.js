
const outputColor = (total_num, select_num) =>{
    //initialize color

    let selected_color = 0;

    //calculate quotient

    let quotient = Math.floor(total_num / 7) + 1;

    if (select_num === 0 ){
        selected_color =0;
    }
    else if (selected_num < )

}